<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hodanny.app1" >
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <meta-data android:name="Windows.package.identity.name" android:value="3967thho2.m1dfthho2arcadia2" />
        <meta-data android:name="Windows.package.identity.publisher" android:value="CN=DCC13F5D-04C3-4744-BA32-05E6311190D9" />
        <meta-data android:name="Windows.publisher.display.name" android:value="thho2" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
